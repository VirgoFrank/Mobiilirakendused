<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:d="http://xamarin.com/schemas/2014/forms/design"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             x:Class="FirstForms.RegisterPage">
    <ContentPage.Content>
        <StackLayout>
            <Entry Placeholder="email" Text="{Binding Email}"></Entry>
            <Entry Placeholder="Password" Text="{Binding Password}"></Entry>
            <Entry Placeholder="First name" Text="{Binding First_Name}"></Entry>
            <Entry Placeholder="Last name" Text="{Binding Last_Name}"></Entry>
            <Button Text="register"
                Clicked="Register"></Button>
        </StackLayout>
    </ContentPage.Content>
</ContentPage>